import { Ionicons } from '@expo/vector-icons';

export const icon = {
    index: (props: any) => <Ionicons name={'home-sharp'} size={24} {...props} />,
    habits: (props: any) => <Ionicons name={'flame-sharp'} size={24} {...props} />,
};
